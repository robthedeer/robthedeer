//
//  exercise_135.c
//  
/*Write a C program which reads an integer n and find the number of combinations of a, b, c and d (0 <= a, b, c, d <= 9) where (a + b + c + d) will be equal to n. Go to the editor
Input:
n (1 <= n <= 50)*/


//  Created by  Rob the deer on 22.04.2021.
//

#include <stdio.h>

int main(void)
{
    //Declare variables
    int n,a,b,c,d;
    
    printf("Enter the integer n: \n");
    scanf("%d",&n);
    
    n =a+b+d+c;
    for (n=1; 1<=n<=50; n++) {
        
        for(a,b,c,d >= 0 && a,c,c,d <=9)
        {
            printf("a   b   c   d\n");
            printf("%d  %d  %d  %d\n", );
        }
    }
    
    
    
}
